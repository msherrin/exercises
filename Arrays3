module.exports.addItem = function(item, list) {
  var groceries = list;
  if (groceries.indexOf(item)) {
  groceries.push(item);
  }
  return groceries;
}
  

module.exports.reverseSortedList = function(p) {
  var array = p;
  return array.sort().reverse();
  }
